app-template:
  controllers:
    main:
      containers:
        sonarr:
          image: { repository: docker.io/linuxserver/sonarr, tag: 4.0.20 }
          securityContext:
            runAsUser: 0
          env:
            PUID: 1029
            PGID: 100
            UMASK: 002
  service:
    main:
      controller: main
      ports:
        sonarr:
          port: 8989
          protocol: HTTP
  ingress:
    main:
      hosts:
        - host: &sonarrHost sonarr.malford.io
          paths:
            - path: /
              pathType: Prefix
              service:
                identifier: main
                port: sonarr
      tls:
        - secretName: sonarr-tls-certificate
          hosts:
            - *sonarrHost
  persistence:
    media:
      type: persistentVolumeClaim
      existingClaim: jellyfin-media
      globalMounts:
        - path: /media
    config:
      advancedMounts:
        main:
          sonarr:
            - path: /config
              subPath: sonarr
    downloads:
      enabled: true
      type: persistentVolumeClaim
      existingClaim: downloads-shared
      advancedMounts:
        main:
          sonarr:
            - path: /downloads
