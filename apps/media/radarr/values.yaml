app-template:
  controllers:
    main:
      containers:
        radarr:
          image: { repository: docker.io/linuxserver/radarr, tag: 6.4.4 }
          securityContext:
            runAsUser: 0
          env:
            PUID: 1029
            PGID: 100
            UMASK: 002
  service:
    main:
      controller: main
      ports:
        radarr:
          port: 7878
          protocol: HTTP
  ingress:
    main:
      hosts:
        - host: &radarrHost radarr.malford.io
          paths:
            - path: /
              pathType: Prefix
              service:
                identifier: main
                port: radarr
      tls:
        - secretName: radarr-tls-certificate
          hosts:
            - *radarrHost
  persistence:
    media:
      type: persistentVolumeClaim
      existingClaim: jellyfin-media
      globalMounts:
        - path: /media
    config:
      advancedMounts:
        main:
          radarr:
            - path: /config
              subPath: radarr
    downloads:
      enabled: true
      type: persistentVolumeClaim
      existingClaim: downloads-shared
      advancedMounts:
        main:
          radarr:
            - path: /downloads
