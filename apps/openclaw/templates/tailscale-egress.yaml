apiVersion: v1
kind: Service
metadata:
  name: aperture
  namespace: {{ .Release.Namespace }}
  annotations:
    tailscale.com/tailnet-ip: "100.95.164.65"
    argocd.argoproj.io/compare-options: IgnoreExtraneous
spec:
  type: ExternalName
  externalName: placeholder  # operator will overwrite this
  ports:
    - port: 80
      protocol: TCP
