apiVersion: v1
kind: ConfigMap
metadata:
  name: openclaw-config
  namespace: {{ .Release.Namespace }}
data:
  openclaw.json: |
    {
      "gateway": {
        "bind": "lan",
        "port": 18789
      }
    }
