apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
  namespace: {{ .Release.Namespace }}
data:
  openclaw.json: |
    {
      "gateway": {
        "bind": "lan",
        "port": 18789
      }
    }
